'use client';

import MediaLibraryPage from '@/app/(protected)/media-library/page';

// Re-use the existing media library page in the admin area
export default function AdminMediaPage() {
  return <MediaLibraryPage />;
}
